import './bootstrap';

// Alpine.js is automatically initialized by Livewire 3.
// We can add custom logic here if needed.
document.addEventListener('alpine:init', () => {
    // Custom Alpine logic
});
